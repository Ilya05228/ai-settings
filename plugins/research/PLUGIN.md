## research

Универсальный “research pack” для поиска/документации/браузинга.

Рекомендуемое наполнение:

- `mcp/` — пресеты MCP (поиск, docs, браузер)
- `agents/` — агенты “researcher”, “docs lookup”, “summarizer”
- `rules/` — правила поведения (источники, цитирование, ограничения)
